public with sharing class FormatUsername {
    // Format the username to include the domain
    public static String formatUsername(String username){
        return username + '@sfafsproject2.username';
    }
}